meta {
  name: Fetch Me
  type: http
  seq: 5
}

get {
  url: {{host_api}}/users/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

script:post-response {
  bru.setVar("authorized_user_id", res.body._id);
}

settings {
  encodeUrl: true
  timeout: 0
}
