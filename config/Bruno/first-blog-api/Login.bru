meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{host_api}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "Tanyaa.ischenko+1@gmail.com",
    "password": "Tanya241996$$"
  }
}

script:post-response {
  bru.setVar("access_token", res.body.access_token);
  bru.runRequest("Fetch Me");
}

settings {
  encodeUrl: true
  timeout: 0
}
